set -g mouse on
set -g extended-keys on
set -g focus-events on
set -g allow-passthrough on


# Pi ask-user tmux notification (BEL -> window flag + terminal beep, same as Claude)
# NOTE: do NOT set visual-bell on -- that swallows the audible terminal beep.
set -g monitor-bell on
